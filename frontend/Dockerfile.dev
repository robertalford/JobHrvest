FROM node:20-alpine
WORKDIR /app
# Install deps as a layer — only re-runs when package.json changes
COPY package*.json ./
RUN npm ci
# Source is mounted as a volume at runtime — no COPY needed
CMD ["npm", "run", "dev", "--", "--host"]
